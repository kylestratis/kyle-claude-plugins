---
description: Execute an implementation plan with beads/deciduous tracking
argument-hint: <plan-dir> <workdir>
---

# Execute

**Arguments:** `$ARGUMENTS`

Use your Skill tool to engage the `executing` skill. Follow it exactly as written.

This command wraps `/ed3d-plan-and-execute:execute-implementation-plan` with integrated beads task tracking and deciduous decision journaling.
