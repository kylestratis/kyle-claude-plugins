---
description: Import features from a roadmap or list into beads tasks (and optionally Linear)
argument-hint: <@file.md | "feature1" "feature2" ...> [--linear] [--team <name>]
---

# Intake

**Arguments:** `$ARGUMENTS`

Use your Skill tool to engage the `intake` skill. Follow it exactly as written.

This command parses features from a markdown file or inline arguments, creates beads epics/tasks, and optionally syncs to Linear.
