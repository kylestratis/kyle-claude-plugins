---
description: Initialize beads and deciduous tracking in current project
argument-hint: [--beads-only | --deciduous-only] [--with-guidance]
---

# Project Init

**Arguments:** `$ARGUMENTS`

Use your Skill tool to engage the `project-init` skill. Follow it exactly as written.

This command sets up task tracking (beads) and decision journaling (deciduous) in the current project.
