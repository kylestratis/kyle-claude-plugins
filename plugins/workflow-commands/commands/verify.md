---
description: Verify implementation with tests, linters, pre-commit, and code review
argument-hint: [--skip-tests] [--skip-lint] [--skip-precommit] [--skip-review] [--task <beads-id>]
---

# Verify

**Arguments:** `$ARGUMENTS`

Use your Skill tool to engage the `verifying` skill. Follow it exactly as written.

This command runs after implementation to ensure quality before commit/PR.
